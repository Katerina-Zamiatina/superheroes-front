import styles from './Header.module.scss';

const Header = () => {
  return (
    <div className={styles.header}>
      <header>
        <h1>SUPERHEROES</h1>
      </header>
    </div>
  );
};

export default Header;
